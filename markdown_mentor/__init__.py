"""Make Markdown Library and Markdown Mentor.

This package has two public command-line tools:

- `make-markdown-library` makes one structured Markdown library file from source
  files, folders, or ZIPs.
- `markdown-mentor` uses a Markdown library file to guide the teaching
  workflow and export finished Markdown materials.

Markdown Mentor can also make a numbered project folder so users know where
each source, prompt, draft, style file, and export belongs.

Markdown Mentor does not talk to any AI itself. The thinking steps are done by
you and an AI chatbot, using the prompts in your project folder or on the
project website.
"""

__version__ = "0.1.5"
