"""Markdown Mentor: turn teaching files into teaching materials, with the help of AI.

This package does the parts a computer is good at:

- build a content pack from your source files (the "build-pack" command)
- check whether the pack is ready to teach from (the "check-pack" command)
- turn finished Markdown materials into Word, PowerPoint, and web files
  (the "export" command)

The thinking steps in between are done by you and an AI chatbot, using the
prompts on the project website. Markdown Mentor does not talk to any AI itself.
"""

__version__ = "0.1.0"
