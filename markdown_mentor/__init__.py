"""Markdown Library Maker and Markdown Mentor.

This package has two public command-line tools:

- `markdown-library` makes one structured Markdown library file from source
  files, folders, or ZIPs.
- `markdown-mentor` uses a Markdown library/content pack to guide the teaching
  workflow and export finished Markdown materials.

Markdown Mentor does not talk to any AI itself. The thinking steps are done by
you and an AI chatbot, using the prompts on the project website.
"""

__version__ = "0.1.0"
